# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# ── Виртуальное окружение / uv ─────────────────────────────
.venv/
# uv.lock НЕ игнорируем — он должен быть в репозитории (фиксирует версии)

# ── Секреты и локальная конфигурация ──────────────────────
.env
.env.local
*.local

# ── ML-модели и аудио (тяжёлые, временные) ────────────────
*.wav
*.mp3
models/
backend/uploads/

# ── IDE / инструменты / ОС ────────────────────────────────
.idea/
.vscode/
.claude/
.DS_Store

# ── Frontend (появится позже) ─────────────────────────────
node_modules/
frontend/dist/
frontend/build/
